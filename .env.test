# Test Environment Configuration
NODE_ENV=test
PORT=3001
HOST=localhost

# Test Database (use a separate test database)
DATABASE_URL=postgresql://username:password@localhost:5432/nebula_test

# Test-specific settings
LOG_LEVEL=error
LOG_PRETTY=false

# Disable rate limiting in tests
RATE_LIMIT_MAX=1000
RATE_LIMIT_WINDOW_MS=1000
